#!/usr/bin/env bash
#
# gmail-jobs — Convenience wrapper for checking background job status
#
# Usage: gmail-jobs [options]
#   See gmail-bg-status.sh --help for options
#
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/gmail-bg-status.sh" "$@"
